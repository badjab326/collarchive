<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Collarchive</title>
</head>
<body>
<header><h1 id="indexTitle">COLLARCHIVE</h1></header>
<p id="flavorText">The Collectors Archive</p>
<nav>
    <form action="/collection/new">
        <input type="submit" value="ADD NEW" />
      </form>
</nav>
<ul id="index">
    <% collections.sort().forEach(collection => { %>
    <li>
        <a href="/collection/<%=collection._id; %>"> <img class="indexImg" src="<%= collection.img %>" title="<%= collection.name %>"> </a>
        <br/>
        <%= collection.name %>
    </li>
    <% }) %>
</ul>
</body>
</html>