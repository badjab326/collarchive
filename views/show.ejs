<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Collarchive</title>
</head>
<body>
    <header><h1 id="showTitle"><%=collection.name %></h1></header>
    <nav>
        <!-- <a href="/collection">Home</a> -->
        <form action="/collection">
            <input type="submit" value="HOME" />
          </form>
        <form action="/collection/<%= index %>/edit">
            <input type="submit" value="EDIT" />
          </form>
    </nav>
    <div id="showMain">
        <img id="showImg" src="<%= collection.img %>" title="<%= collection.name %>">
    </div>
        <br />
        <br />
        <h2 id="platform"><%= collection.platform %></h2>
        <br />
        <p id="info"><%= collection.description %></p>
        <p id="qty"><%= collection.qty %> Owned</p>

        <br />
        <form id="delete" action="/collection/<%= index %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE" />
        </form>
</body>
</html>