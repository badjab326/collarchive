<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Collarchive</title>
</head>
<body>
    <header><h1 id="showTitle"><%=collection.name %></h1></header>
    <nav>
        <!-- <a href="/collection">Home</a> -->
        <form action="/collection">
            <input type="submit" value="HOME" />
          </form>
        <form action="/collection/<%= index %>/edit">
            <input type="submit" value="EDIT" />
          </form>
    </nav>
    <img src="<%= collection.img %>" title="<%= collection.name %>">
    <br />
    <br />
    <h2><%= collection.platform %></h2>
    <p><%= collection.description %></p>
    <p><%= collection.qty %> Owned</p>

    <br />
    <form action="/collection/<%= index %>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE" />
      </form>
</body>
</html>